base:
  random_seed: 43
  project: infmax/infmax-gnn
  run_id: IN-163
  nb_seeds: 2

data:
  protocol: OR
  output_label_name:
    - exposed
    - simulation_length
    - peak_iteration
    - peak_infected
  output_weights:
    w_e: 4
    w_sl: 1
    w_pit: 1
    w_pin: 1
  networks:
    - name: lazega
      features_type: zeros
    - name: toy_network
      features_type: zeros
    - name: lazega
      features_type: zeros
