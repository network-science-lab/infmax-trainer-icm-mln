hydra:
  run:
    dir: _data_results/model_outputs/${now:%Y-%m-%d}/${now:%H-%M-%S}

# experiment-config: tavg_and # BASELING
# experiment-config: best_or

# final
# experiment-config: e_variant_h
# experiment-config: e_variant_i
experiment-config: e_variant_j
# experiment-config: a_domain
# experiment-config: t_none
# experiment-config: loss_mae
# experiment-config: or

# features experiments
# experiment-config: f_cent
# experiment-config: f_ones
# experiment-config: f_zeros

# transforms experiments
# experiment-config: t_actor_num
# experiment-config: t_contract_and_norm
# experiment-config: t_contract_with_log
# experiment-config: t_norm_and_scatter
# experiment-config: t_scatter_exponent

# agregations experiments
# experiment-config: a_att
# experiment-config: a_avg
# experiment-config: a_max
# experiment-config: a_min
# experiment-config: a_sum
# experiment-config: a_linear

# hidden channels experiments
# experiment-config: h_200
# experiment-config: h_600
# experiment-config: h_800

# encoder experiments
# experiment-config: e_variant_e
# experiment-config: e_variant_f
# experiment-config: e_variant_g
# experiment-config: e_variant_d
# experiment-config: e_variant_c

# experiment-config: config_and
# experiment-config: config_or
